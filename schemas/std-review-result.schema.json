{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "STD Review Result",
  "description": "Schema for QualityFlow STD review agent output. Validated by harness before postscript runs.",
  "type": "object",
  "required": ["status", "jira_id", "verdict", "confidence", "weighted_score", "findings", "report"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["success", "error"]
    },
    "jira_id": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9]+-\\d+$"
    },
    "verdict": {
      "type": "string",
      "enum": ["APPROVED", "APPROVED_WITH_FINDINGS", "NEEDS_REVISION"]
    },
    "confidence": {
      "type": "string",
      "enum": ["HIGH", "MEDIUM", "LOW"]
    },
    "weighted_score": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "findings": {
      "type": "object",
      "required": ["critical", "major", "minor", "actionable", "total"],
      "properties": {
        "critical": { "type": "integer", "minimum": 0 },
        "major": { "type": "integer", "minimum": 0 },
        "minor": { "type": "integer", "minimum": 0 },
        "actionable": { "type": "integer", "minimum": 0 },
        "total": { "type": "integer", "minimum": 0 },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["finding_id", "severity", "dimension", "description", "remediation", "actionable"],
            "properties": {
              "finding_id": { "type": "string" },
              "severity": { "type": "string", "enum": ["CRITICAL", "MAJOR", "MINOR"] },
              "dimension": { "type": "string" },
              "description": { "type": "string", "minLength": 10, "maxLength": 2000 },
              "evidence": { "type": "string" },
              "remediation": { "type": "string", "minLength": 10, "maxLength": 2000 },
              "actionable": { "type": "boolean" }
            }
          }
        }
      }
    },
    "dimension_scores": {
      "type": "object",
      "properties": {
        "traceability": { "type": "integer", "minimum": 0, "maximum": 100 },
        "yaml_structure": { "type": "integer", "minimum": 0, "maximum": 100 },
        "pattern_matching": { "type": "integer", "minimum": 0, "maximum": 100 },
        "step_quality": { "type": "integer", "minimum": 0, "maximum": 100 },
        "content_policy": { "type": "integer", "minimum": 0, "maximum": 100 },
        "pse_quality": { "type": "integer", "minimum": 0, "maximum": 100 },
        "codegen_readiness": { "type": "integer", "minimum": 0, "maximum": 100 }
      }
    },
    "artifacts_reviewed": {
      "type": "object",
      "properties": {
        "std_yaml": { "type": "boolean" },
        "go_stubs": { "type": "boolean" },
        "python_stubs": { "type": "boolean" },
        "stp_available": { "type": "boolean" }
      }
    },
    "report": {
      "type": "string",
      "description": "Path to the review report markdown"
    }
  },
  "if": {
    "properties": { "status": { "const": "error" } }
  },
  "then": {
    "required": ["status", "jira_id", "reason"],
    "properties": {
      "reason": {
        "type": "string",
        "enum": ["std-not-found", "yaml-invalid", "config-error", "timeout"]
      }
    }
  }
}
